Николай, привет!

Наименование пакетов должно отражать суть содержимого. Переименуй sprintone в pageobgects.
- исправлено

Создавать отдельный класс для тестовых данных имеет смысл, когда мы описываем модель данных.
В даннном случае используется массив и вынесение массива в отдельный класс усложняет код.
Перенеси тестовые данные в тестовый класс.
- исправлено

Патерн Page Object включает веб-элементы и шаги (действия или методы взаимодействия между
веб-элементами для получения актуального результата). Для шагов не нужно создавать
отдельный класс. Перенеси шаги в MainPage
- исправлено

Для веб-элементов используй модификатор доступа private.
- пробовал ставить private, но только после этой модификации код перестал работать,
как его реанимировать, в топиках спринта информации не нашел, буду признателен подсказке

Локаторы не следует описывать через цепочку div элементов с индексами. Такие локаторы
сложно поддерживать. Локатор должен быть легко читаем. Это сократит время локализации дефекта.
Исправь все некорректные локаторы.
- локаторы, описанные таким образом, в моем проекте относятся только к блоку информации
о характеристиках самоката, где все элементы имеют один общий класс = Home_Column__xlKDK. Если не использовать
структуру, предложенную мной, то я вижу только вариант зацепиться за сам текст: //*[text()='Проедет без поздарядки'].
но тут возникают сложности с полем с заголовком блока - //*[text()='&nbsp;Модель Toxic PRO']
локатор не работает ни в таком виде, ни с пробелом, ни без оного. Для меня тупиковая ситуация.

В данной версии Selenide действительно нет возможности запуска в полноэкранном режиме, но Selenide это обертка над Selenium и мы можем использовать его методы:
WebDriverRunner.getWebDriver().manage().window().maximize();
- спасибо, внедрил